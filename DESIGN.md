# DESIGN.md

TasteReview의 화면 디자인 단일 출처. 색·폰트·여백 값은 여기서만 정의하고, 템플릿과 CSS는 이 토큰만 참조한다.

## 원칙

1. **모바일 우선.** 기준 폭은 360px이며, 넓은 화면은 확장으로 다룬다. 가로 스크롤은 어떤 폭에서도 허용하지 않는다.
2. **따뜻한 색감.** 중립 회색 대신 붉은기·노란기가 섞인 색을 쓴다. 음식 사진이 화면의 주인공이므로 UI는 사진을 받쳐 준다.
3. **Pretendard가 메인 폰트.** 제목과 본문 모두 한 서체로 통일하고 굵기로 위계를 만든다.
4. **여백은 8px 배수.** 패딩·마진·간격에 8의 배수만 쓴다. 4px 이하 미세 조정은 쓰지 않고, 필요하면 1px 테두리나 line-height로 해결한다.

## 색 토큰

`:root`에 CSS 커스텀 프로퍼티로 선언하고, 값을 다른 곳에 다시 적지 않는다.

| 토큰 | 값 | 용도 | 배경 대비 |
| --- | --- | --- | --- |
| `--color-bg` | `#FDF8F3` | 페이지 배경 (따뜻한 오프화이트) | - |
| `--color-surface` | `#FFFFFF` | 카드·폼 표면 | - |
| `--color-border` | `#E8DDD1` | 구분선, 입력 테두리 | - |
| `--color-text` | `#3A2E26` | 본문 (따뜻한 다크브라운) | 12.4:1 |
| `--color-text-muted` | `#7A6A5D` | 보조 정보, 작성일 | 4.9:1 |
| `--color-primary` | `#B24A28` | 주요 버튼, 링크 (테라코타) | 5.1:1 |
| `--color-primary-hover` | `#8F3B1F` | 주요 버튼 hover/active | - |
| `--color-primary-soft` | `#F7E6DC` | 선택된 필터 배경 | - |
| `--color-rating` | `#C77D14` | 별점 아이콘 (앰버) | 3.1:1 |
| `--color-success` | `#4F6B3A` | 등록 성공 배너 | 4.6:1 |
| `--color-danger` | `#A33228` | 오류 메시지, 삭제 버튼 | 5.9:1 |
| `--color-focus` | `#B24A28` | 포커스 링 |  - |

다크 모드는 `@media (prefers-color-scheme: dark)`에서 같은 토큰 이름을 재정의한다. JavaScript 토글은 두지 않는다(ADR 0001).

| 토큰 | 다크 값 |
| --- | --- |
| `--color-bg` | `#1B1613` |
| `--color-surface` | `#241D19` |
| `--color-border` | `#3A312B` |
| `--color-text` | `#F3EAE2` |
| `--color-text-muted` | `#B9A899` |
| `--color-primary` | `#E0764A` |
| `--color-primary-soft` | `#3A241A` |
| `--color-rating` | `#E8A33D` |

본문 텍스트는 4.5:1 이상, 아이콘·테두리는 3:1 이상을 유지한다. 새 색을 추가할 때 이 수치를 먼저 확인한다.

## 타이포그래피

Pretendard는 버전을 고정한 CDN 스타일시트로 불러오고, 실패 시 시스템 서체로 자연히 대체된다.

```
font-family: "Pretendard Variable", Pretendard, -apple-system, BlinkMacSystemFont,
             system-ui, "Apple SD Gothic Neo", "Malgun Gothic", sans-serif;
```

| 역할 | 크기 | 굵기 | line-height |
| --- | --- | --- | --- |
| 페이지 제목 | 1.5rem (24px) | 700 | 1.3 |
| 카드 제목 | 1.125rem (18px) | 600 | 1.4 |
| 본문 | 1rem (16px) | 400 | 1.6 |
| 보조 정보 | 0.875rem (14px) | 400 | 1.5 |
| 배지·캡션 | 0.75rem (12px) | 500 | 1.4 |

`html`의 기준 크기는 16px로 두고 크기는 rem으로 쓴다. **입력 요소의 글자 크기는 16px 미만으로 내리지 않는다.** iOS Safari가 16px 미만 입력에 포커스될 때 화면을 확대해 레이아웃이 깨진다.

## 여백

| 토큰 | 값 | 주 용도 |
| --- | --- | --- |
| `--space-1` | 8px | 아이콘과 글자, 배지 내부 |
| `--space-2` | 16px | 카드 내부 패딩, 좌우 거터 |
| `--space-3` | 24px | 폼 항목 사이, 카드 사이 |
| `--space-4` | 32px | 섹션 사이 |
| `--space-5` | 48px | 페이지 상하 여백 |
| `--space-6` | 64px | 빈 상태 화면 위아래 |

8의 배수가 아닌 값은 쓰지 않는다. 테두리(1px)와 포커스 링(2px)만 예외다.

## 레이아웃과 반응형

- 콘텐츠 컨테이너는 `max-width: 720px`, 좌우 패딩 16px, 가운데 정렬. 읽기 폭을 넘기지 않는 편이 리뷰 본문 가독성에 유리하다.
- 브레이크포인트는 세 개만 둔다. 기본(모바일) → `min-width: 600px`(태블릿) → `min-width: 960px`(데스크톱).
- 리뷰 목록은 모바일 1열, 600px 이상 2열, 960px 이상 3열.
- 사진은 `aspect-ratio: 4 / 3`, `object-fit: cover`로 고정해 로딩 중 레이아웃이 밀리지 않게 한다.
- 이미지에는 `loading="lazy"`와 의미 있는 `alt`를 넣는다.

## 컴포넌트 규칙

| 컴포넌트 | 규칙 |
| --- | --- |
| 버튼 | 최소 높이 48px, 패딩 `16px 24px`, radius 8px. 주요 동작은 `--color-primary` 채움, 보조 동작은 테두리만, 삭제는 `--color-danger` |
| 터치 대상 | 버튼·링크·페이지 번호는 최소 44×44px 확보. 목록의 페이지 번호가 가장 자주 이 규칙을 어긴다 |
| 입력 | 높이 48px, 패딩 `8px 16px`, 테두리 1px, radius 8px. 라벨은 항상 표시하고 placeholder로 대체하지 않는다 |
| 오류 표시 | 색만으로 전달하지 않는다. 문장과 함께 해당 입력 아래에 둔다 |
| 카드 | 패딩 16px, radius 8px, 테두리 1px. 그림자는 쓰지 않는다 |
| 별점 | `--color-rating` 아이콘 + 숫자 병기. 색만으로 값을 전달하지 않는다 |
| 지역 배지 | 12px 글자, 패딩 `8px`, radius 8px, `--color-primary-soft` 배경 |
| 배너 | 등록·삭제 결과 메시지. 리다이렉트 후 한 번 표시된다(SRS-F-14) |
| 빈 상태 | 검색 결과 0건일 때 안내 문장과 조건 초기화 링크(SRS-F-28) |
| 포커스 | `:focus-visible`에 2px 외곽선. 키보드 사용자를 위해 제거하지 않는다 |

## 구현 제약

- 화면은 서버 렌더링이므로 CSS만으로 완결한다(ADR 0001). 스타일을 위한 JavaScript는 쓰지 않는다.
- 스타일은 정적 리소스의 단일 CSS 파일에 두고, 템플릿에 인라인 `style` 속성을 쓰지 않는다.
- 클래스 이름은 `블록__요소` 형태로 쓴다. 예: `review-card__title`, `search-form__field`.
- 색·크기·여백 리터럴을 CSS 규칙 본문에 직접 적지 않는다. 토큰이 없으면 먼저 이 문서에 토큰을 추가한다(ADR 0006과 같은 이유다).

## 변경 이력

| 날짜 | 변경 내용 |
| --- | --- |
| 2026-09-17 | 최초 작성. 모바일 우선·따뜻한 색감·Pretendard·8px 배수 원칙 확정 |
